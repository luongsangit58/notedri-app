diff --git a/node_modules/react-native-ble-plx/android/build.gradle b/node_modules/react-native-ble-plx/android/build.gradle
index 4f1fd5d..1b5fbbe 100644
--- a/node_modules/react-native-ble-plx/android/build.gradle
+++ b/node_modules/react-native-ble-plx/android/build.gradle
@@ -1,14 +1,3 @@
-buildscript {
-  repositories {
-    google()
-    mavenCentral()
-  }
-
-  dependencies {
-    classpath "com.android.tools.build:gradle:8.8.0"
-  }
-}
-
 def isNewArchitectureEnabled() {
   return rootProject.hasProperty("newArchEnabled") && rootProject.getProperty("newArchEnabled") == "true"
 }
diff --git a/node_modules/react-native-ble-plx/android/src/main/java/com/bleplx/adapter/BleModule.java b/node_modules/react-native-ble-plx/android/src/main/java/com/bleplx/adapter/BleModule.java
index 44392b6..7dedd93 100644
--- a/node_modules/react-native-ble-plx/android/src/main/java/com/bleplx/adapter/BleModule.java
+++ b/node_modules/react-native-ble-plx/android/src/main/java/com/bleplx/adapter/BleModule.java
@@ -1065,8 +1065,20 @@ public class BleModule extends ReactContextBaseJavaModule implements BleAdapter
       .subscribe(onAdapterStateChangeCallback::onEvent);
   }
 
+  // NoteDri patch: PackageManager.FEATURE_BLUETOOTH_LE is the documented Android
+  // API for this check, but several cheap Android car head-unit ROMs (Unisoc/
+  // Allwinner "car stereo" AOSP builds) never declare it even though the radio
+  // genuinely supports BLE (confirmed: same units already pair Bluetooth
+  // Classic audio fine). Trusting only the manifest flag falsely tells users
+  // "this device doesn't support Bluetooth BLE" on hardware that would actually
+  // work. Fall back to "adapter exists" when the flag is false - virtually
+  // every Bluetooth radio shipped since the BLE era (2011+) supports both
+  // Classic and LE on the same chip, so a present BluetoothAdapter is a good
+  // enough signal; a truly LE-incapable device just fails later at scan/connect
+  // with a clear error instead of being blocked upfront by a buggy ROM flag.
   private boolean supportsBluetoothLowEnergy() {
-    return context.getPackageManager().hasSystemFeature(PackageManager.FEATURE_BLUETOOTH_LE);
+    return context.getPackageManager().hasSystemFeature(PackageManager.FEATURE_BLUETOOTH_LE)
+      || bluetoothAdapter != null;
   }
 
   @BluetoothState
diff --git a/node_modules/react-native-ble-plx/plugin/build/withBLEAndroidManifest.js b/node_modules/react-native-ble-plx/plugin/build/withBLEAndroidManifest.js
index 94b349f..790ea08 100644
--- a/node_modules/react-native-ble-plx/plugin/build/withBLEAndroidManifest.js
+++ b/node_modules/react-native-ble-plx/plugin/build/withBLEAndroidManifest.js
@@ -21,30 +21,14 @@ exports.withBLEAndroidManifest = withBLEAndroidManifest;
  *    From Android SDK 31 (Android 12) it might not be required if BLE is not used for location.
  */
 function addLocationPermissionToManifest(androidManifest, neverForLocationSinceSdk31) {
-    if (!Array.isArray(androidManifest.manifest['uses-permission-sdk-23'])) {
-        androidManifest.manifest['uses-permission-sdk-23'] = [];
-    }
-    const optMaxSdkVersion = neverForLocationSinceSdk31
-        ? {
-            'android:maxSdkVersion': '30'
-        }
-        : {};
-    if (!androidManifest.manifest['uses-permission-sdk-23'].find(item => item.$['android:name'] === 'android.permission.ACCESS_COARSE_LOCATION')) {
-        androidManifest.manifest['uses-permission-sdk-23'].push({
-            $: {
-                'android:name': 'android.permission.ACCESS_COARSE_LOCATION',
-                ...optMaxSdkVersion
-            }
-        });
-    }
-    if (!androidManifest.manifest['uses-permission-sdk-23'].find(item => item.$['android:name'] === 'android.permission.ACCESS_FINE_LOCATION')) {
-        androidManifest.manifest['uses-permission-sdk-23'].push({
-            $: {
-                'android:name': 'android.permission.ACCESS_FINE_LOCATION',
-                ...optMaxSdkVersion
-            }
-        });
-    }
+    // PATCHED (notedri-app, patches/react-native-ble-plx+3.5.1.patch): bỏ hẳn bước tự thêm
+    // ACCESS_COARSE_LOCATION/ACCESS_FINE_LOCATION dạng <uses-permission-sdk-23> - app.json đã có
+    // plugin "expo-location" tự thêm đúng 2 quyền này dạng <uses-permission> thường rồi. Google
+    // Play từ chối AAB lúc upload vì báo "trùng lặp với các maxSdkVersion khác" khi CÙNG 1 quyền
+    // xuất hiện dưới cả 2 dạng phần tử khác nhau trong manifest cuối cùng (dù cả 2 đều không đặt
+    // maxSdkVersion tường minh) - loại bỏ hẳn dạng sdk-23 ở nguồn thay vì cố dọn dẹp lại bằng 1
+    // config plugin chạy sau (thử nhưng thứ tự mod của `expo prebuild` giữa các plugin không ổn
+    // định/đáng tin cậy để đảm bảo luôn chạy SAU withBLEAndroidManifest).
     return androidManifest;
 }
 /**
